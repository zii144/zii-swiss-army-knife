{
  "name": "@zii/calc",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Calculation & units engine — finance, dates, units, cooking, and currency helpers. Pure, offline, deterministic.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run",
    "lint": "eslint ."
  }
}
