{
  "name": "@zii/hello-tool",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Sample tool that proves the registry + lazy-loading pipeline (M1 smoke test).",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run",
    "lint": "eslint ."
  },
  "dependencies": {
    "@zii/registry": "workspace:*"
  }
}
