{
  "name": "@zii/registry",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Tool registry + lazy plugin loader for the Zii utility suite.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run",
    "lint": "eslint ."
  }
}
