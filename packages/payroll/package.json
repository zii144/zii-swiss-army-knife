{
  "name": "@zii/payroll",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Payroll & Tax Engine — M9 pluggable per-jurisdiction rule modules, progressive income tax, and VAT/GST sales tax. Pure, offline, deterministic.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run",
    "lint": "eslint ."
  },
  "dependencies": {
    "@zii/locale": "workspace:*"
  }
}
